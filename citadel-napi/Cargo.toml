[package]
name = "citadel-napi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["default"] }
napi-derive = "2"
citadel-core = { path = "../citadel-core" }
serde_json = "1"

[build-dependencies]
napi-build = "2"
